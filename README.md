github.io
=========

To install dependencies:
```
bundle install
```

To generate site [locally](http://0.0.0.0:4000/):
```
bundle exec jekyll serve
```

To publish to GitHub Pages:

Push to the `source` branch. GitHub Actions will automatically build and deploy to the `main` branch.
